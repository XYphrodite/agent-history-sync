## Исправлено

- Сборка `v0.17.0` падала на Linux (`server-linux`): `SessionManagerWindow.axaml` использовал `StringFormat` в привязке `LastModifiedAt`, который не компилируется Avalonia XAML на `ubuntu-latest`. Форматирование убрано — дата выводится как есть (совместимо с Linux сборкой `CodexHistorySync.Desktop`).

## Совместимость и ограничения

- Без изменений относительно `v0.17.0`. Требуется Windows для окна `--manage`.

## Обновление

```powershell
agent-sync update
```
