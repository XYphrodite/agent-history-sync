## Исправлено

- `SessionManagerTests` падал на `ubuntu-latest`/`windows-latest`: `RefreshAsync` перезаписывал статус `Cannot read this session` и `Copied to …` на `2 of 2 sessions`. Исправлено сохранение статуса после `SelectAsync`/`RefreshAsync` для нечитаемых и скопированных сессий.

## Совместимость

- Без изменений формата.

## Обновление

```powershell
agent-sync update
```
