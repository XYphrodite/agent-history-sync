## Исправлено

- Сборка `v0.17.1` падала на `ubuntu-latest` (`server-linux`): `SessionManagerWindow.axaml.cs` не хватало `using Avalonia.Input.Platform` / `CodexHistorySync.Core.Viewing` для `IClipboard.SetTextAsync` и `TraceSearchMatch`, а `SessionManagerApp.cs` — `using Avalonia.Controls` для `ShutdownMode`. Добавлены импорты — `dotnet build` Desktop/Cli теперь собирается на Linux, `Server.Tests` снова зелёный.

## Совместимость

- Без изменений формата, требуется Windows для окна `--manage`.

## Обновление

```powershell
agent-sync update
```
