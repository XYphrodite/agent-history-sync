## Обновление

- Самообновление переведено на общий `SelfUpdateKit` (`XYphrodite/self-update-kit`): обязательный SHA-256, probe staged-бинаря с откатом, чистка retired-копий. Поведение `agent-sync update` не изменилось.
- Сообщение при 404 теперь называет репозиторий и лечение (нет релиза / переименован / приватный), а не пустой результат поиска.

## Обновление

```powershell
agent-sync update
```
