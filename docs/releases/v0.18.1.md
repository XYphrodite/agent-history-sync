## Исправление

- **Linux-сборка (`server-linux`)** падала с `CS0246: SessionScanResult` / `CS7036: SessionCatalogSnapshot` / `CS0103: museHome` из-за неполного мерджа Muse-ветки (пропущены `using CodexHistorySync.Core.Codex`, `MuseSessionCatalogSource` на новом `ISessionCatalogIo`/`SessionCatalogReadLimiter.RunAsync`, `PathSafety.EnsureSessionDestination` без `musePaths`, `LocalSessionOperations`/`CodexHistoryWriter`/`CoreCliSyncRuntime` без шестого слота, тесты с 4–5 аргументами `SessionCatalogSnapshot`). Все шесть слотов (`Codex/Grok/Claude/Continue/Kimi/Muse`) теперь собираются на `ubuntu-latest` и `windows-latest`.

- **MuseTests** исправлен: `muse-pack-<uuid>` → `<uuid>` внутри `muse-pack-<rand>/`.

## Обновление

```powershell
agent-sync update
```
